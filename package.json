{
	"name": "flivitycli",
	"version": "1.0.0",
	"description": "A CLI to translate between languages in the terminal",
	"type": "module",
	"main": "./build/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"package": "pkg . --targets node14-macos-x64 --output flivitycli",
		"create": "npm run build && npm run package"
	},
	"keywords": [
		"cli"
	],
	"bin": {
		"flivitycli": "./build/index.js"
	},
	"author": "Your Name",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^17.0.8",
		"pkg": "^5.5.1",
		"typescript": "^4.5.4"
	},
	"pkg": {
		"scripts": "build/**/*.js"
	},
	"dependencies": {
		"commander": "^8.3.0"
	}
}
